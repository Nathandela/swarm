# Final audit committee: Remote Control playbook delivery (R0-R9)

Five-member adversarial committee - codex (GPT-5.6 sol), Gemini 3.5 Flash, Sonnet, Opus,
and Fable (standing owner instruction) - convened 2026-08-21 against
docs/specifications/remote-control-product-playbook.md, iterating to unanimity over five
rounds. UNANIMOUS APPROVAL at main 34db9db.

## The approved claim

1. The delivery is code-complete and CI-green per amended playbook section 9, with its
   per-wave scoping (R1-R4's named open code work; R5 physical-only; R8 = read half per
   ADR-017 C0, control half open in agents-tracker-huia). Every committee code finding
   from rounds 1-5 is fixed and mutation-proven, with exactly two carve-outs, each in
   the form its own finder prescribed: j0jh ("a probe, not a patch") and v94i (a bead
   was the demand).
2. The ledger keeps its own rule across all wave beads: hggx.2-.10 open and
   dependency-blocked by named field-exit/remainder beads; hggx.1 closed with its R0
   orphan caveat recorded and carried by ev0w; the campaign order is bead 6eu0; every
   correction the committee forced is recorded in place with its history.
3. The owner starts the field-exit campaign from bead 6eu0.

## What the committee changed (commits fc58601..34db9db, all CI-green)

Round 1 rejected the delivery STATEMENT (tracker contradicted "all closed"; the velocity
policy lived only in prose; two new code defects). Round 2 verified every round-1 fix by
re-breaking it, then caught the correction's own gaps (three wave beads closed against
the new rule; a TOCTOU race in the desync fix). Round 3 approved the ledger and produced
the unified-ledger prescription. Round 4 verified everything and found the
honest-double-timeout corner (idleLeak counter). Round 5 found the clamp double-billing
and prescribed the license-conservation rule verbatim.

Code the committee forced: hello/caps wait negotiation (phone AND gateway), the unified
abandonment ledger through three correctness iterations (skip -> discard -> counted
licenses), AckBatcher adoption, CloseNow backgrounding, the jx1x and onPause main-thread
fixes with extended gates, R7 containment edges + startup-kill replay + markLost reap +
reap mutex, tag publication gated on the container scan, the Stop-presence fence, the
x/crypto bump, and the F7 load-tolerant deadline family. Ledger/governance: playbook
section 9 and the R8 wave section amended per section 16, nine wave beads brought to
graph-enforced truth, the 6eu0 campaign epic, and ~30 beads filed/corrected/closed with
evidence.

Every fence named here is mutation-proven; the mutation records live in
docs/verification/committee-fixes/ and the per-round verdicts in the session record.
