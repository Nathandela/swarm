# S25 machines screen: failing-first run (GG-5)

Recorded before ui/kit/PresenceDot.kt, ui/screens/MachinesPanel.kt and
ui/screens/MachinesPanelView.kt existed. The two gate rows that name them were written first --
`presenceDot` in `s23Inbox` (android/gate/s23_kit_test.go) and the MachinesPanelView.kt entry in
`s24ScreenComponents` (android/gate/s24_screens_test.go) -- and this is what they said with
nothing behind them.

THE ACTIVITY ROWS IN THIS OUTPUT ARE NOT MINE. `ActivityRow.kt` and `ActivityPanelView.kt` are a
second agent's rows, landing in the same two tables while this ran. They are left in rather than
filtered out: the run is what it was, and a doctored capture is worth less than a noisy one.

## Command

    go test ./android/gate/ -run "TestPBDS6_EveryInboxComponentIsAKitFactory|TestPBDS7_EveryDerivationCitationResolvesToARow|TestPBDS6_EveryKitFileIsClaimedByAFence|TestPBDS6_EveryRecomposedScreenIsBuiltOutOfTheKit" -v

    HEAD b06cec9, branch design-system-substrate, 2026-08-01T16:29:45Z

## Output

```
=== RUN   TestPBDS6_EveryInboxComponentIsAKitFactory
    s23_kit_test.go:421: PB-DS-6: the kit has no ActivityRow.kt, which is where activityRow() lives.
        	the activity feed's only structural element, and the machine pane's audit log -- one factory for both, which is why it takes a body and an optional emphasis rather than a JournalRow. Substrate's demo phone renders the inbox and nothing else, so `.arow` is the retired mock's class and §4 is the whole specification. Its card geometry is `.prow`'s and is spent through cardSurface -- §2's reuse rule again, and the reason the mock's radius 12 and 11/13 padding are not here. The mock's fixed 52 px timestamp gutter is dropped with NOTHING replacing it: 52 is not on the spacing scale, and `HH:MM` in a monospace face is constant width by construction. That dropped column is also what lets the timestamp be optional, which it has to be -- see the note on s23DerivedSpacing below.
    s23_kit_test.go:421: PB-DS-6: the kit has no PresenceDot.kt, which is where presenceDot() lives.
        	the machines screen's 7 dp mark, and it is a SEPARATE FACTORY FROM statusDot rather than a fifth key in the Group table. `--p-ok` online and `--p-ink3` offline is the same pair of tokens two Groups already carry, so the cheap implementation is to hand statusDot the Group whose colour happens to match -- `ready_for_review` for online, `completed` for offline -- which renders every pixel correctly and is the phone INVENTING a status.Group. TestPBDS7_TheStatusDotBindingIsTheCheckedInMapping refuses a bound name that is not in android/group-tokens.tsv precisely so that cannot be done quietly, and the row's own words are that machine presence is not a Group and must not be smuggled in as one. So the drawable, the 7 dp and the flat no-glow treatment are shared with statusDot and the BINDING is not: this one takes a boolean, and the Group fence stays strict.
--- FAIL: TestPBDS6_EveryInboxComponentIsAKitFactory (0.01s)
=== RUN   TestPBDS6_EveryKitFileIsClaimedByAFence
    s23_kit_test.go:532: PB-DS-6: PB-DS-6/PB-DS-7, by s23OwnedFiles in this file claims dev/swarm/phone/ui/kit/ActivityRow.kt and no such file exists. A claim over nothing makes the forward direction above pass by having less to check: the scans skip the missing name, report no fault, and the inventory goes on describing a kit that is not there.
    s23_kit_test.go:532: PB-DS-6: PB-DS-6/PB-DS-7, by s23OwnedFiles in this file claims dev/swarm/phone/ui/kit/PresenceDot.kt and no such file exists. A claim over nothing makes the forward direction above pass by having less to check: the scans skip the missing name, report no fault, and the inventory goes on describing a kit that is not there.
--- FAIL: TestPBDS6_EveryKitFileIsClaimedByAFence (0.00s)
=== RUN   TestPBDS7_EveryDerivationCitationResolvesToARow
--- PASS: TestPBDS7_EveryDerivationCitationResolvesToARow (0.00s)
=== RUN   TestPBDS6_EveryRecomposedScreenIsBuiltOutOfTheKit
    s24_screens_test.go:1197: PB-DS-6: 2 screens are not built out of the kit:
        dev/swarm/phone/ui/screens/ActivityPanelView.kt: the composition table claims this screen and the file does not exist, so its components are checked against nothing
        dev/swarm/phone/ui/screens/MachinesPanelView.kt: the composition table claims this screen and the file does not exist, so its components are checked against nothing
--- FAIL: TestPBDS6_EveryRecomposedScreenIsBuiltOutOfTheKit (0.01s)
FAIL
FAIL	github.com/Nathandela/swarm/android/gate	0.767s
FAIL
```

## What changed after this run, and why the quoted prose does not match the tree

The `Why` text quoted above for the `presenceDot` row was rewritten after this capture, and the
row's citation moved from `§4 Machine presence dot` to `#11 Machine row`. The reason is not a
second thought about the component: commit e8af092 ("Revert the duplicate section 4 rows: rows
11-15 already said all of it") removed the §4 adjacent rows this slice was briefed against, and
row 11 -- which was always there -- states the same presence mark, its two tokens and its
flatness. The citation was pointed at the surviving row.

That mismatch was itself caught by a fence rather than by reading. With PresenceDot.kt written and
the stale citation still in it:

```
    s23_kit_test.go:574: PB-DS-7: PresenceDot.kt cites `§4 Machine presence dot`, and no such row
    exists in docs/design/substrate-components.md. Either the row was renamed and the component now
    paints to a specification nobody can find, or the citation was written from memory.
```

which is TestPBDS7_EveryDerivationCitationResolvesToARow doing exactly the job its comment claims.

## What is NOT in this file, and why

The KOTLIN lane's red run. Three suites were written before their subjects existed --
`PresenceDotTest`, `MachinesPanelTest` and `MachinesPanelViewTest` -- and their failing-first
state is a COMPILE failure (`unresolved reference: presenceDot`, `unresolved reference:
machinesPanelView`), not a test report. Capturing it needs `./gradlew`, which this agent was told
not to run: the Android lane is sequenced by whoever holds it, so the JVM half of this slice's
evidence is theirs to record.

Recorded as an absence rather than as a run that happened. What is above is the whole of what was
executed here.

## The green run

    go test ./android/gate/

is recorded by whoever runs the lane after the Kotlin half compiles. The two rows above were
observed to go green locally after the three files landed; that run is not in this file, because
this file is the RED one.
